FROM node:20-slim

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY . .

RUN chown -R node:node /app

ENV NODE_ENV=production
ENV PORT=8000

EXPOSE 8000

USER node

CMD ["node", "server.js"]